# URL base del backend (sin slash final)
# En desarrollo local déjalo vacío — el proxy de Vite redirige /api → localhost:3001
VITE_API_URL=

# Para Azure (producción), apunta al Container App del backend:
# VITE_API_URL=https://tu-backend.azurecontainerapps.io
